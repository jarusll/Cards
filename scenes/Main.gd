extends Node

const CardFactory = preload('res://scripts/CardFactory.gd')

func _ready():
	pass
	# var ability = CardFactory.new().make('ability')
	# var character = CardFactory.new().make('character')
	# $Interface/Enemies.add_child(character)
	# $Interface/Enemies.add_child(ability)
	# $Enemies.add_child(character)
	# $Enemies.add_child(character)
	# self.add_child(ability)
	# self.add_child(character)
